# Teacup
Library based on pygame designed for building games quickly.
Refer to examples.py for usage.

### Installation instructions:
1. Click code and download as zip, unzip once downloaded
2. Open terminal of choice and navigate to the unziped folder
3. Run `pip install .`
4. To verify installation run `pip list` and check if "teacup" is in the list
