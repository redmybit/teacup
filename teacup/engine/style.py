def get_style_template() -> dict:
    """
    Returns the default style templated used by all objects
    """

    return {
        "color": (0, 0, 0, 255),
        "background-color": (0, 0, 0, 0),
        "font-size": 24,
        "font-family": "Inter",
    }
